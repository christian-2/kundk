#!/bin/bash
set -e
trap 'echo "error: $0:$LINENO"' ERR

cd $(dirname $0)/..

podman volume rm state &> /dev/null || true
podman volume create state &> /dev/null

podman kube play --replace k8s/base.yaml
podman kube play --replace k8s/demo.yaml
